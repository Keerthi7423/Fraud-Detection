
====================================================
PART 5 — DAY TO DAY TASKS (35 DAYS)
Realistic plan for working professionals
2-3 hours per day after your job at SkySecure
====================================================

How to use this:
  Open this file every morning before coding
  Read ONLY that day's tasks
  Check off each task as you finish
  Do NOT move to next day until current day is done
  2-3 hours per day minimum
  30 mins LeetCode EVERY morning before starting — no exceptions

Legend:
  [ ] = Task to complete
  [*] = Critical — do not skip
  [!] = Common mistake — read carefully
  [~] = Optional if you have extra time


====================================================
PHASE 1 — BACKEND FOUNDATION (Days 1-14)
Build all 4 microservices one by one
====================================================


----------------------------------------------------
DAY 1 — PROJECT SETUP
Time: 2 hours
Goal: GitHub repo ready, all 4 folders created, MongoDB Atlas connected
----------------------------------------------------

  [ ] Create GitHub repo: fraudguard-microservices
  [ ] Clone to computer
  [ ] Create folder structure:
        fraudguard-microservices/
        ├── client/
        ├── services/
        │   ├── auth-service/
        │   ├── transaction-service/
        │   ├── ai-scoring-service/
        │   └── notification-service/
        ├── infrastructure/
        └── README.md

  [ ] Create root .gitignore:
        node_modules
        .env
        dist
        .DS_Store

  [ ] Go to cloud.mongodb.com — create FREE account
  [ ] Create free M0 cluster
  [ ] Create DB user with username + password
  [ ] Copy connection string — save it somewhere safe:
        mongodb+srv://youruser:yourpass@cluster0.xxxxx.mongodb.net/fraudguard
  [ ] Whitelist all IPs: Network Access → Add 0.0.0.0/0

  [ ] Initialize auth-service:
        cd services/auth-service
        npm init -y
        npm install express mongoose dotenv jsonwebtoken bcryptjs joi morgan helmet cors
        npm install --save-dev nodemon
        Add to package.json scripts: "dev": "nodemon src/server.js"
        Create src/server.js — basic express on port 3001 with /health route

  [ ] Test: npm run dev → http://localhost:3001/health → should return ok

  [*] COMMIT: "Day 1: Project setup and auth-service initialized"

  END CHECK:
  [ ] GitHub repo exists with first commit
  [ ] MongoDB Atlas cluster running
  [ ] Auth service starts on port 3001 without errors


----------------------------------------------------
DAY 2 — AUTH SERVICE PART 1 (User Model + DB)
Time: 2-3 hours
Goal: MongoDB connected, User model working
----------------------------------------------------

  [ ] Create services/auth-service/src/config/db.js:
        const mongoose = require('mongoose')
        const connectDB = async () => {
          await mongoose.connect(process.env.MONGO_URI)
          console.log('MongoDB connected')
        }
        module.exports = connectDB

  [ ] Create services/auth-service/.env:
        PORT=3001
        MONGO_URI=your_mongodb_connection_string/fraudguard
        JWT_SECRET=fraudguard_secret_key_2025
        NODE_ENV=development

  [ ] Call connectDB() in server.js before app.listen()

  [ ] Create services/auth-service/src/models/User.js:
        Fields: name, email, password, role (analyst/admin default analyst),
                isActive (default true), lastLogin, timestamps
        Pre-save hook: hash password with bcrypt (rounds 12)
        Method: comparePassword(candidate) returns bcrypt.compare result

  [ ] Test: restart server → should see "MongoDB connected" in terminal
  [ ] Check MongoDB Atlas → fraudguard database should appear

  [*] COMMIT: "Day 2: MongoDB connected, User model created"


----------------------------------------------------
DAY 3 — AUTH SERVICE PART 2 (Register + Login)
Time: 2-3 hours
Goal: Register and login endpoints working
----------------------------------------------------

  [ ] Create src/controllers/authController.js with register():
        Validate body with Joi: name (required), email (required), password (min 6), role
        Check if email exists → 409 conflict error
        Create new User → save
        Return 201: { message: 'Registered successfully', user: { name, email, role } }
        NEVER return password field

  [ ] Add login() to authController.js:
        Validate email + password
        Find user by email → if not found → 401
        user.comparePassword(password) → if false → 401
        Generate token: jwt.sign({ id: user._id, role: user.role }, JWT_SECRET, { expiresIn: '8h' })
        Update user.lastLogin = new Date() → save
        Return 200: { token, user: { id, name, email, role } }

  [ ] Add getMe() to authController.js:
        Find user by req.user.id → select('-password')
        Return user object

  [ ] Create src/routes/authRoutes.js:
        POST /register → authController.register
        POST /login    → authController.login
        GET  /me       → protect middleware (tomorrow) + authController.getMe

  [ ] Register in server.js: app.use('/auth', authRoutes)

  [ ] Test in Postman:
        POST http://localhost:3001/auth/register
        Body: { "name":"Keerthi", "email":"k@test.com", "password":"Test@123", "role":"admin" }
        Should return 201

        POST http://localhost:3001/auth/login
        Body: { "email":"k@test.com", "password":"Test@123" }
        Should return token

  [*] COMMIT: "Day 3: Register and login endpoints working"


----------------------------------------------------
DAY 4 — AUTH SERVICE PART 3 (JWT Middleware + Complete)
Time: 2 hours
Goal: Auth service 100% complete and tested
----------------------------------------------------

  [ ] Create src/middleware/authMiddleware.js:
        Get token from header: req.headers.authorization?.split(' ')[1]
        If no token → return 401 { error: 'No token provided' }
        Verify: const decoded = jwt.verify(token, process.env.JWT_SECRET)
        Set req.user = decoded
        Call next()
        Catch jwt errors → return 401 { error: 'Invalid token' }

  [ ] Create src/middleware/roleMiddleware.js:
        If req.user.role !== 'admin' → return 403 { error: 'Admin access required' }
        Else call next()

  [ ] Create src/middleware/errorHandler.js:
        (err, req, res, next) => res.status(err.status || 500).json({ error: err.message })

  [ ] Add errorHandler as LAST middleware in server.js

  [ ] Update /me route to use protect middleware:
        router.get('/me', protect, authController.getMe)

  [ ] Create 1 analyst + 1 admin account via Postman — save both tokens

  [ ] Test all scenarios:
        /me with valid token → 200
        /me without token → 401
        /me with wrong token → 401

  [*] COMMIT: "Day 4: Auth service complete with JWT middleware"

  END CHECK: Auth service is 100% done. Do not touch it again.


----------------------------------------------------
DAY 5 — TRANSACTION SERVICE PART 1 (Setup + Model)
Time: 2-3 hours
Goal: Transaction service running with model ready
----------------------------------------------------

  [ ] Initialize transaction-service:
        cd services/transaction-service
        npm init -y
        npm install express mongoose dotenv jsonwebtoken joi morgan helmet cors
        npm install @aws-sdk/client-sqs
        npm install --save-dev nodemon
        Create src/server.js on port 3002

  [ ] Create .env:
        PORT=3002
        MONGO_URI=same_mongodb_uri/fraudguard
        JWT_SECRET=fraudguard_secret_key_2025  (same as auth)
        AWS_REGION=ap-south-1
        AWS_ACCESS_KEY_ID=placeholder_for_now
        AWS_SECRET_ACCESS_KEY=placeholder_for_now
        SQS_SCORING_QUEUE_URL=placeholder_for_now
        NODE_ENV=development

  [ ] Copy authMiddleware.js from auth-service into this service
        (Same JWT_SECRET = tokens work across both services)

  [ ] Create src/config/db.js — same as auth service
  [ ] Connect DB in server.js

  [ ] Create src/models/Transaction.js with full schema:
        transactionId (String, unique, required)
        amount (Number, required)
        currency (String, default INR)
        merchantName (String, required)
        merchantCategory (String, required)
        cardLastFour (String)
        cardType (String, enum: credit/debit)
        location: { city, country, ipAddress }
        userId (String)
        timestamp (Date, required)
        status (String, enum: pending/clean/suspicious/fraudulent, default: pending)
        riskScore (Number, default: 0)
        riskLevel (String, enum: low/medium/high/critical)
        aiReasons ([String])
        aiRecommendation (String)
        reviewedBy (String)
        reviewedByName (String)
        reviewedAt (Date)
        reviewNote (String)
        scoringStatus (String, enum: queued/scoring/scored, default: queued)
        timestamps: true

  [ ] Add index: TransactionSchema.index({ transactionId:1, timestamp:-1, status:1, riskScore:-1 })

  [*] COMMIT: "Day 5: Transaction service setup and model created"


----------------------------------------------------
DAY 6 — TRANSACTION SERVICE PART 2 (SQS + Stripe Setup)
Time: 2-3 hours
Goal: SQS client ready, Stripe API integration started
----------------------------------------------------

  [ ] Create src/config/sqsClient.js:
        const { SQSClient } = require('@aws-sdk/client-sqs')
        module.exports = new SQSClient({ region: process.env.AWS_REGION })

  [ ] Create src/services/queueService.js:
        async function sendToScoringQueue(transaction) {
          if (process.env.NODE_ENV === 'development') {
            console.log('DEV: Would send to SQS:', transaction.transactionId)
            return
          }
          // Real SQS code here
        }
        module.exports = { sendToScoringQueue }

  [ ] Get Stripe API Keys (Test Mode):
        Go to dashboard.stripe.com → Developers → API keys
        Copy Publishable key and Secret key
  [ ] Add to .env:
        STRIPE_SECRET_KEY=sk_test_...
        STRIPE_WEBHOOK_SECRET=whsec_... (get this from stripe listen)

  [ ] Install stripe package: npm install stripe

  [*] COMMIT: "Day 6: SQS and Stripe setup complete"


----------------------------------------------------
DAY 7 — TRANSACTION SERVICE PART 3 (Stripe Webhook Handler)
Time: 3 hours
Goal: Stripe payments trigger the fraud detection flow
----------------------------------------------------

  [ ] Create src/controllers/webhookController.js:
        Handle POST /webhook:
        const sig = req.headers['stripe-signature']
        const event = stripe.webhooks.constructEvent(req.body, sig, STRIPE_WEBHOOK_SECRET)
        
        If event.type === 'payment_intent.succeeded':
          const pi = event.data.object
          Extract transaction data:
            id: pi.id
            amount: pi.amount / 100
            currency: pi.currency
            card: pi.payment_method_details.card (last4, brand)
            customer: pi.customer
          Save to Transaction model in MongoDB
          Call sendToScoringQueue(transaction)

  [ ] Add webhook route to transactionRoutes.js:
        router.post('/webhook', express.raw({type: 'application/json'}), handleWebhook)

  [ ] Install Stripe CLI to test locally:
        stripe listen --forward-to localhost:3002/transactions/webhook
        stripe trigger payment_intent.succeeded

  [*] COMMIT: "Day 7: Stripe webhook integration complete"

        updateTransactionStatus():
          const { status, reviewNote } = req.body
          Validate: reviewNote is required (min 10 chars)
          Validate: status must be approved/rejected/escalated
          Map to DB status: approved→clean, rejected→fraudulent, escalated→suspicious
          Update transaction with status, reviewedBy, reviewedByName, reviewedAt, reviewNote
          Return updated transaction

        getReviewQueue():
          Find where status is 'suspicious' OR status is 'pending'
          Sort by riskScore descending
          Return array

        getDashboardStats():
          Today = start of today (midnight)
          total = Transaction.countDocuments({ createdAt: { $gte: today } })
          flagged = Transaction.countDocuments({ status: {$in:['suspicious','fraudulent']}, createdAt:{$gte:today} })
          confirmed = Transaction.countDocuments({ status:'fraudulent', reviewedAt:{$gte:today} })
          saved = Transaction.aggregate sum of amount where status fraudulent today
          Return { total, flagged, confirmed, saved }

        getFraudTrends():
          Last 7 days fraud count per day using $group aggregation
          Return array of { date, count }

        getFraudByCategory():
          Group by merchantCategory where status fraudulent
          Return array of { category, count }

        getFraudByHour():
          Group by hour of day where status fraudulent
          Return array of { hour, count }

  [ ] Create src/routes/transactionRoutes.js with all routes
  [ ] All routes protected with authMiddleware
  [ ] Register in server.js: app.use('/transactions', transactionRoutes)

  [*] COMMIT: "Day 7: Transaction service CRUD controller complete"


----------------------------------------------------
DAY 8 — TRANSACTION SERVICE COMPLETE + TEST
Time: 2-3 hours
Goal: All transaction endpoints tested and working
----------------------------------------------------

  [ ] Test every endpoint in Postman (use admin token from Day 4):
        POST http://localhost:3002/transactions (empty body = mock)
        GET  http://localhost:3002/transactions
        GET  http://localhost:3002/transactions?status=pending
        GET  http://localhost:3002/transactions?category=electronics
        GET  http://localhost:3002/transactions?minAmount=50000
        GET  http://localhost:3002/transactions/:transactionId
        GET  http://localhost:3002/transactions/queue
        PATCH http://localhost:3002/transactions/:id
              Body: { "status":"rejected", "reviewNote":"Looks fraudulent" }
        GET  http://localhost:3002/transactions/stats
        GET  http://localhost:3002/transactions/trends

  [ ] Create 10 mock transactions via Postman
  [ ] Verify they appear in MongoDB Atlas (Compass or Atlas UI)
  [ ] Fix any bugs found

  [~] Create a simple test script: src/seed.js
        Creates 20 mock transactions automatically
        Run: node src/seed.js

  [*] COMMIT: "Day 8: Transaction service fully tested"

  END CHECK: Transaction service is 100% done.


----------------------------------------------------
DAY 9 — GEMINI API SETUP + TEST
Time: 2 hours
Goal: Gemini scoring working as standalone test
----------------------------------------------------

  [ ] Initialize ai-scoring-service:
        cd services/ai-scoring-service
        npm init -y
        npm install express mongoose dotenv @google/generative-ai
        npm install @aws-sdk/client-sqs
        npm install --save-dev nodemon
        Create src/server.js on port 3003

  [ ] Get FREE Gemini API key:
        Go to https://aistudio.google.com/app/apikey
        Sign in with Google account → Create API Key
        Copy key

  [ ] Create .env:
        PORT=3003
        MONGO_URI=same_mongodb_uri/fraudguard
        GEMINI_API_KEY=your_gemini_key_here
        AWS_REGION=ap-south-1
        AWS_ACCESS_KEY_ID=placeholder
        AWS_SECRET_ACCESS_KEY=placeholder
        SQS_SCORING_QUEUE_URL=placeholder
        SQS_NOTIFICATION_QUEUE_URL=placeholder
        NODE_ENV=development

  [ ] Create src/config/geminiClient.js:
        const { GoogleGenerativeAI } = require('@google/generative-ai')
        const genAI = new GoogleGenerativeAI(process.env.GEMINI_API_KEY)
        module.exports = genAI.getGenerativeModel({ model: 'gemini-1.5-flash' })

  [ ] Create src/services/aiScoringService.js
        COPY EXACT CODE from Part 4.3 of this PRD

  [ ] Create src/test-gemini.js to test scoring:
        require('dotenv').config()
        const { scoreTransaction } = require('./services/aiScoringService')
        scoreTransaction({
          transactionId: 'TXN-TEST1',
          amount: 89000,
          merchantName: 'Apple Store',
          merchantCategory: 'electronics',
          location: { city: 'Dubai', country: 'UAE' },
          timestamp: new Date('2025-01-02T02:30:00Z'),
          cardType: 'credit',
          cardLastFour: '9821'
        }).then(r => console.log(JSON.stringify(r, null, 2)))

  [ ] Run: node src/test-gemini.js
  [ ] Should return:
        {
          riskScore: 85-95,
          riskLevel: "high" or "critical",
          aiReasons: [...3 reasons...],
          aiRecommendation: "block"
        }

  [ ] Test with a low risk transaction too:
        amount: 250, merchantName: 'Zomato', category: 'food',
        city: 'Bengaluru', time: 12:30 PM
        Should return riskScore: 5-20, riskLevel: "low"

  [*] COMMIT: "Day 9: Gemini API integrated and tested"


----------------------------------------------------
DAY 10 — AI SCORING SERVICE COMPLETE (SQS Worker)
Time: 3 hours
Goal: Scoring worker polls queue and updates transactions
----------------------------------------------------

  [ ] Copy Transaction model from transaction-service into ai-scoring-service
        (Scoring service needs to UPDATE transactions after scoring)

  [ ] Create src/config/db.js — connects to same fraudguard DB

  [ ] Create src/config/sqsClient.js — same as transaction service

  [ ] Create src/workers/scoringWorker.js
        COPY EXACT CODE from Part 4.4 of this PRD

  [!] For now NODE_ENV=development so SQS is not real yet.
      The worker will try to connect to SQS but fail silently.
      That is OK — you will connect real SQS on Day 29.
      For now just verify the worker starts without crashing.

  [ ] In server.js after connectDB():
        require('./workers/scoringWorker')
  [ ] Start service: npm run dev
  [ ] Should see: "AI Scoring Worker started — polling SQS every 5s"
  [ ] Worker will log "no messages" every 5 seconds — that is correct

  [*] COMMIT: "Day 10: AI Scoring service with SQS worker complete"


----------------------------------------------------
DAY 11 — NOTIFICATION SERVICE COMPLETE
Time: 2-3 hours
Goal: Audit log service writing to MongoDB
----------------------------------------------------

  [ ] Initialize notification-service:
        cd services/notification-service
        npm init -y
        npm install express mongoose dotenv @aws-sdk/client-sqs
        npm install --save-dev nodemon
        Create src/server.js on port 3004

  [ ] Create .env:
        PORT=3004
        MONGO_URI=same_mongodb_uri/fraudguard
        AWS_REGION=ap-south-1
        AWS_ACCESS_KEY_ID=placeholder
        AWS_SECRET_ACCESS_KEY=placeholder
        SQS_NOTIFICATION_QUEUE_URL=placeholder
        NODE_ENV=development

  [ ] Create src/config/db.js
  [ ] Create src/config/sqsClient.js

  [ ] Create src/models/AuditLog.js:
        transactionId (String, required)
        analystId (String)
        analystName (String)
        action (String, enum: approved/rejected/escalated/auto-flagged)
        note (String)
        previousStatus (String)
        newStatus (String)
        riskScore (Number)
        timestamp (Date, default: now)
        source (String, enum: analyst/system)

  [ ] Create src/workers/notificationWorker.js:
        Polls SQS_NOTIFICATION_QUEUE_URL every 5 seconds
        For each message:
          Parse JSON body
          Create new AuditLog from message data
          Save to MongoDB
          Delete message from SQS queue
          Log: "Audit logged: " + transactionId + " → " + action

  [ ] Create src/controllers/auditController.js:
        getAllAuditLogs(): Find all, sort by timestamp desc, return array
        getTransactionAuditLog(): Find by transactionId, return array

  [ ] Copy authMiddleware.js from auth-service
  [ ] Create routes + register in server.js
  [ ] Start worker in server.js same as scoring service

  [*] COMMIT: "Day 11: Notification service and audit log complete"


----------------------------------------------------
DAY 12 — DOCKER SETUP
Time: 2-3 hours
Goal: All 4 services run with one docker-compose command
----------------------------------------------------

  [ ] Install Docker Desktop: https://www.docker.com/products/docker-desktop

  [ ] Create Dockerfile in each of 4 service folders:

        FROM node:20-alpine
        WORKDIR /app
        COPY package*.json ./
        RUN npm ci --only=production
        COPY src/ ./src/
        EXPOSE [PORT]
        CMD ["node", "src/server.js"]

        Ports: auth=3001, transaction=3002, scoring=3003, notification=3004

  [ ] Create infrastructure/docker-compose.yml:
        version: '3.8'
        services:
          auth-service:
            build: ../services/auth-service
            ports: ["3001:3001"]
            env_file: ../services/auth-service/.env

          transaction-service:
            build: ../services/transaction-service
            ports: ["3002:3002"]
            env_file: ../services/transaction-service/.env
            depends_on: [auth-service]

          ai-scoring-service:
            build: ../services/ai-scoring-service
            ports: ["3003:3003"]
            env_file: ../services/ai-scoring-service/.env

          notification-service:
            build: ../services/notification-service
            ports: ["3004:3004"]
            env_file: ../services/notification-service/.env

  [ ] Run: cd infrastructure && docker-compose up --build
  [ ] All 4 containers should start (takes 2-3 min first time)
  [ ] Test /health on all 4 ports
  [ ] Fix any Docker build errors (usually missing packages)

  [*] COMMIT: "Day 12: Docker and docker-compose complete"


----------------------------------------------------
DAY 13 — BACKEND END-TO-END TEST + BUG FIX
Time: 2-3 hours
Goal: Full backend flow tested, zero known bugs
----------------------------------------------------

  [ ] Start all services via docker-compose
  [ ] Do full Postman test of every endpoint:
        Auth: register, login, /me
        Transactions: create, list, filter, detail, update, queue, stats
        Audit: list, by transaction id
  [ ] Create 20 mock transactions
  [ ] Manually update 5 transactions to suspicious
  [ ] Verify audit logs appear for each update
  [ ] Test with invalid data: missing fields, wrong token, wrong role
  [ ] Fix every bug found

  [*] COMMIT: "Day 13: Full backend integration test complete"


----------------------------------------------------
DAY 14 — AWS ACCOUNT + SQS SETUP
Time: 2-3 hours
Goal: Real AWS SQS queues created and working
----------------------------------------------------

  [ ] Create FREE AWS account at aws.amazon.com
        Credit card required (you will NOT be charged for our services)
        Select Basic Support (free)
        Set region to ap-south-1 (Mumbai)

  [ ] Create IAM User:
        IAM → Users → Create User
        Name: fraudguard-deploy
        Attach policy: AdministratorAccess
        Create access key → save both values

  [ ] Install AWS CLI: https://aws.amazon.com/cli/
  [ ] Run: aws configure
        Enter Access Key ID and Secret
        Region: ap-south-1
        Format: json
  [ ] Test: aws sts get-caller-identity → should show account ID

  [ ] Create SQS queues:
        AWS Console → SQS → Create Queue
        Name: fraudguard-transaction-scoring-queue (Standard type)
        Name: fraudguard-notification-queue (Standard type)
        Copy both URLs

  [ ] Update all 4 .env files with real AWS values:
        AWS_ACCESS_KEY_ID=real_key
        AWS_SECRET_ACCESS_KEY=real_secret
        SQS_SCORING_QUEUE_URL=real_url
        SQS_NOTIFICATION_QUEUE_URL=real_url

  [ ] Remove DEV MODE skip from queueService.js in transaction-service
  [ ] Restart all services
  [ ] Create a transaction → check SQS console → message should appear
  [ ] Start scoring service → message consumed → transaction scored in DB

  [*] COMMIT: "Day 14: Real AWS SQS integrated — async scoring working"

  PHASE 1 COMPLETE. All backend services done and talking to each other.


====================================================
PHASE 2 — REACT FRONTEND (Days 15-25)
====================================================


----------------------------------------------------
DAY 15 — REACT SETUP + REDUX + LOGIN PAGE
Time: 3 hours
Goal: React app with working login
----------------------------------------------------

  [ ] cd client
  [ ] npm create vite@latest . -- --template react
  [ ] npm install react-router-dom axios
  [ ] npm install @reduxjs/toolkit react-redux
  [ ] npm install recharts lucide-react react-hot-toast
  [ ] npm install @tanstack/react-table react-datepicker react-gauge-chart
  [ ] npm install -D tailwindcss postcss autoprefixer
  [ ] npx tailwindcss init -p

  [ ] Configure tailwind.config.js content array
  [ ] Add Tailwind directives to index.css
  [ ] Add Inter + JetBrains Mono fonts via Google Fonts in index.html

  [ ] Create .env:
        VITE_AUTH_URL=http://localhost:3001
        VITE_TRANSACTION_URL=http://localhost:3002
        VITE_AUDIT_URL=http://localhost:3004

  [ ] Create src/services/api.js:
        Two Axios instances: authAPI and transactionAPI
        Both use request interceptor to attach JWT from localStorage
        Both use response interceptor: on 401 → clear token → redirect to /login

  [ ] Create src/store/store.js with configureStore
  [ ] Create src/store/authSlice.js:
        State: { user: null, token: null }
        Actions: setCredentials (saves user + token), logout (clears both)

  [ ] Wrap App in Redux Provider + BrowserRouter in main.jsx

  [ ] Build src/pages/Login.jsx:
        Dark full screen (#0F1117 background)
        Centered card max-width 400px (#1A1D27)
        Shield icon + "FraudGuard" in blue at top
        "Secure. Real-time. Intelligent." tagline in grey
        Email + password inputs (dark bg, light border)
        Show/hide password toggle button
        Login button full width blue
        On submit: call POST /auth/login via authAPI
        On success: dispatch setCredentials, save token to localStorage, push to /dashboard
        On error: red error text below form

  [ ] Create src/components/common/ProtectedRoute.jsx:
        Check localStorage for token
        If no token → redirect to /login
        Else render children

  [ ] Set up routes in App.jsx:
        /login → Login (public)
        /dashboard, /transactions, /queue, /transactions/:id, /audit, /admin
        → all wrapped in ProtectedRoute

  [ ] Create empty placeholder for each protected page
  [ ] Test login with admin credentials from Day 3
  [ ] Should redirect to /dashboard after login

  [*] COMMIT: "Day 15: React setup, Redux, login page working"


----------------------------------------------------
DAY 16 — SIDEBAR + NAVBAR + LAYOUT
Time: 2-3 hours
Goal: Full app shell with working navigation
----------------------------------------------------

  [ ] Build src/components/common/Sidebar.jsx:
        Fixed 240px left sidebar background #13151F
        Logo top: shield icon + "FraudGuard" text in blue
        Nav items with Lucide icons:
          LayoutDashboard → /dashboard
          ArrowLeftRight → /transactions
          AlertTriangle → /queue (with red number badge)
          ClipboardList → /audit
          Settings → /admin (only if role === admin)
        Active item: left 3px blue border + slightly lighter bg
        Hover: 150ms bg transition
        Bottom: user name + role + logout button
        Logout: dispatch logout, clear localStorage, push to /login

  [ ] Build src/components/common/Navbar.jsx:
        Top bar across main content
        Left: current page title (from route)
        Right: Bell icon with alert count + user avatar

  [ ] Create Layout wrapper:
        Sidebar (fixed left) + main content area (flex-1)
        Navbar at top of main content
        Page content below navbar (overflow-y-auto)

  [ ] Update App.jsx to use Layout for protected routes
  [ ] Test all navigation links — correct page loads
  [ ] Test active state highlights correct nav item
  [ ] Test logout clears token and goes to /login

  [*] COMMIT: "Day 16: Sidebar, navbar, layout shell complete"


----------------------------------------------------
DAY 17 — TRANSACTION TABLE COMPONENTS
Time: 3 hours
Goal: Reusable table and badge components built
----------------------------------------------------

  [ ] Build src/components/common/RiskBadge.jsx:
        Props: level (low/medium/high/critical) and status
        Returns colored pill badge with dot
        Low = #052E16 bg + #22C55E text
        Medium = #451A03 bg + #F59E0B text
        High = #450A0A bg + #EF4444 text
        Critical = #3F0000 bg + #DC2626 text + pulse animation

  [ ] Build src/components/common/LoadingSkeleton.jsx:
        Props: rows (default 5)
        Returns animated shimmer bars using Tailwind animate-pulse
        Use in all pages while data is loading

  [ ] Build src/components/transactions/TransactionTable.jsx
        using TanStack Table (@tanstack/react-table)
        Columns:
          Transaction ID (JetBrains Mono font, truncated)
          Amount (₹ formatted with toLocaleString en-IN)
          Merchant Name
          Category
          City
          Time (formatted: "2 Jan, 2:34 AM")
          Risk Score (number + mini colored progress bar)
          Status (RiskBadge component)
          Actions (eye icon → navigate to detail)
        Critical risk rows (score 91+): pulse glow animation
        Row hover: bg lightens
        Pagination: 20 rows, Prev/Next/page numbers
        Sort by clicking column headers (TanStack built-in)

  [*] COMMIT: "Day 17: RiskBadge, Skeleton, TransactionTable components"


----------------------------------------------------
DAY 18 — TRANSACTION FEED PAGE
Time: 3 hours
Goal: Full transaction feed page working with real data
----------------------------------------------------

  [ ] Build src/components/transactions/FilterBar.jsx:
        Status dropdown: All / Clean / Suspicious / Fraudulent / Pending
        Category dropdown: All / Food / Travel / Electronics / Clothing / Fuel / Medical
        Min Amount + Max Amount inputs
        From Date + To Date (react-datepicker)
        Search input (searches transactionId and merchantName)
        Apply Filters button (blue)
        Reset button (grey, clears all filters)

  [ ] Create src/services/transactionService.js:
        getTransactions(filters) → GET /transactions with query params
        getTransaction(id) → GET /transactions/:id
        createTransaction() → POST /transactions (no body = mock)
        updateTransaction(id, data) → PATCH /transactions/:id
        getQueue() → GET /transactions/queue
        getStats() → GET /transactions/stats
        getTrends() → GET /transactions/trends
        getCategories() → GET /transactions/categories
        getHourly() → GET /transactions/hours

  [ ] Build src/pages/TransactionFeed.jsx:
        State: transactions, total, filters, loading, error
        On mount: fetch transactions
        On filter change: re-fetch with new params
        Top bar: FilterBar left + "Simulate Stripe Payment" button right
        Simulate button: hits /transactions/simulate which triggers a Stripe Test Payment,
          shows success toast, auto-refreshes table after 6 seconds
          (wait for async Gemini scoring to complete)
        LoadingSkeleton while data fetching
        TransactionTable below
        Empty state if no results

  [*] COMMIT: "Day 18: Transaction feed page with filters complete"


----------------------------------------------------
DAY 19 — TRANSACTION DETAIL PAGE
Time: 3 hours
Goal: Full detail view with risk gauge and action buttons
----------------------------------------------------

  [ ] Build src/components/transactions/RiskGauge.jsx:
        Uses react-gauge-chart
        Props: score (0-100)
        nrOfLevels: 20
        colors: ['#22C55E', '#F59E0B', '#EF4444']
        Large score number below gauge (JetBrains Mono, colored)
        Risk level text below score

  [ ] Build src/pages/TransactionDetail.jsx:
        Get transactionId from URL param: useParams()
        Fetch transaction on mount
        Show LoadingSkeleton while fetching

        TWO COLUMN LAYOUT (desktop):
          LEFT: Transaction info card
            ID, Amount (large bold ₹ formatted), Merchant,
            Category, Time, Location, Card, Status badge
          RIGHT: AI Analysis card
            RiskGauge with score
            Risk level label (colored)
            "Why Flagged" section:
              Map aiReasons → each as bullet with ⚠ icon
            Recommendation label: approve/review/block

        BELOW BOTH COLUMNS: Analyst Action card
          Label: "Your Decision"
          Textarea: placeholder "Add a note explaining your decision (required)"
          Validate: note must be min 10 characters
          Three buttons:
            [✓ Approve] green → sets status to 'approved' + reviewNote
            [✗ Reject] red → sets status to 'rejected' + reviewNote
            [↑ Escalate] amber → sets status to 'escalated' + reviewNote
          On button click: show window.confirm "Are you sure?"
          On confirm: call updateTransaction() → success toast → navigate to /queue

        If transaction already reviewed:
          Hide action buttons
          Show: "Reviewed by [name] on [date]"
          Show the review note

        Breadcrumb at top: Dashboard > Transactions > TXN-xxxxx

  [*] COMMIT: "Day 19: Transaction detail with gauge and actions"


----------------------------------------------------
DAY 20 — REVIEW QUEUE PAGE
Time: 2 hours
Goal: Queue page with inline actions working
----------------------------------------------------

  [ ] Build ActionModal.jsx component:
        Props: isOpen, transactionId, action, onClose, onConfirm
        Small centered modal with dark overlay
        Shows: "Confirm [action] for TXN-xxxxx"
        Textarea for note (required, min 10 chars)
        [Cancel] and [Confirm] buttons
        Disable confirm until note is filled

  [ ] Build src/pages/ReviewQueue.jsx:
        Fetch from getQueue() on mount
        Page header: "Review Queue" + amber count badge
        Subtext: "Sorted by risk — highest risk first"
        Same TransactionTable but:
          No FilterBar
          Extra column: Actions
          Actions column: [Approve] green btn + [Reject] red btn
          Clicking either opens ActionModal
          On confirm: call updateTransaction → refetch queue
        Empty state:
          Large ✓ icon
          "All caught up! No transactions need review."

  [*] COMMIT: "Day 20: Review queue with inline actions"


----------------------------------------------------
DAY 21 — AUDIT LOG PAGE
Time: 2 hours
Goal: Audit log showing all analyst actions
----------------------------------------------------

  [ ] Create src/services/auditService.js:
        getAuditLogs(filters) → GET /audit (notification service port 3004)
        getTransactionAuditLog(id) → GET /audit/:id

  [ ] Build src/pages/AuditLog.jsx:
        Admin only — if role !== admin, redirect to /dashboard
        Fetch from auditService on mount
        Filter row: analyst name input, action dropdown, date range
        Table columns:
          Timestamp (formatted) | Analyst | Transaction ID | Action | Note
        Action column colored:
          approved → #22C55E green
          rejected → #EF4444 red
          escalated → #F59E0B amber
          auto-flagged → #3B82F6 blue
        LoadingSkeleton while loading
        Empty state if no logs

  [*] COMMIT: "Day 21: Audit log page complete"


----------------------------------------------------
DAY 22 — ADMIN DASHBOARD — STATS CARDS + CHARTS
Time: 3-4 hours
Goal: Full dashboard with live data and charts
----------------------------------------------------

  [ ] Build src/components/dashboard/StatsCard.jsx:
        Props: title, value, subtitle, icon, iconColor
        Dark card (#1A1D27)
        Icon top-right in colored circle
        Large value (JetBrains Mono)
        Subtitle below in grey

  [ ] Build FraudLineChart.jsx (Recharts LineChart):
        X axis: last 7 days (Mon, Tue...)
        Y axis: fraud count
        Line color: #EF4444
        Grid lines: #2A2D3E
        Dark tooltip
        Title: "Fraud Trend — Last 7 Days"

  [ ] Build CategoryPieChart.jsx (Recharts PieChart):
        Data by category
        Colors from chart palette in design PRD
        Legend below
        Title: "Fraud by Category"

  [ ] Build HourlyBarChart.jsx (Recharts BarChart):
        X axis: hours 0-23
        Bars color: #3B82F6
        Title: "Fraud by Hour of Day"

  [ ] Build src/pages/Dashboard.jsx:
        Fetch stats, trends, categories, hourly on mount
        4 StatsCards in a row at top:
          Total Transactions today (blue icon)
          Flagged today (amber icon)
          Confirmed Fraud today (red icon)
          Amount Saved (green icon, ₹ formatted)
        FraudLineChart full width below cards
        Two columns below: CategoryPieChart + HourlyBarChart
        LoadingSkeleton for each section

  [*] COMMIT: "Day 22: Admin dashboard with stats and charts"


----------------------------------------------------
DAY 23 — POLISH: LOADING + ERRORS + MOBILE
Time: 3 hours
Goal: App looks professional on all screen sizes
----------------------------------------------------

  [ ] Add empty states to every page with meaningful messages
  [ ] Add error states: red box + retry button on every page
  [ ] Add loading spinners inside all action buttons while API calls run
  [ ] Add success/error toasts (react-hot-toast) for every user action

  [ ] Fix mobile responsiveness:
        Sidebar: hide on mobile, show hamburger in navbar
        Hamburger click: sidebar slides in as overlay from left
        Click outside closes sidebar
        Stats cards: 2×2 grid on mobile
        Transaction table: hide Location and Card columns on mobile
        Detail page: single column on mobile
        Action buttons: full width stacked on mobile

  [ ] Fix page transition: add subtle fade-in when navigating between pages

  [ ] Add CSV export button to Dashboard:
        Call GET /transactions/export from transaction service
        Trigger file download in browser

  [*] COMMIT: "Day 23: Loading states, errors, mobile responsive, CSV export"


----------------------------------------------------
DAY 24 — SYSTEM HEALTH PAGE + FINAL FEATURES
Time: 2 hours
Goal: Admin panel showing service health status
----------------------------------------------------

  [ ] Build src/pages/AdminPanel.jsx:
        User management section:
          Table: Name | Email | Role | Status | Last Login
          Toggle active/inactive (optional — implement if time allows)

        System Health section (this is what impresses JP Morgan):
          Title: "Microservice Status"
          4 cards, one per service:
            Auth Service      ● Online
            Transaction Svc   ● Online
            AI Scoring Svc    ● Online
            Notification Svc  ● Online
          Each card calls the /health endpoint of that service
          Green dot = 200 response, Red dot = failed/timeout
          Refresh every 30 seconds automatically

  [ ] Add notification badge to Review Queue sidebar link:
        Fetch queue count every 30 seconds
        Show red number badge if count > 0
        Update Redux store with queue count

  [*] COMMIT: "Day 24: Admin panel and system health page"


----------------------------------------------------
DAY 25 — FULL FRONTEND INTEGRATION TEST
Time: 2-3 hours
Goal: Zero bugs before deployment
----------------------------------------------------

  [ ] Start all services + frontend
  [ ] Walk through entire app as a new user:
        1. Register new admin account
        2. Login
        3. Go to Dashboard — verify charts load
        4. Go to Transaction Feed
        5. Generate 10 mock transactions
        6. Wait 5-6 seconds for Gemini scoring
        7. Refresh — check risk scores appeared
        8. Filter by suspicious
        9. Click one to open detail page
        10. Read AI reasons on gauge
        11. Add note and approve
        12. Go to Review Queue — approve 2 more
        13. Reject 1 with a note
        14. Go to Audit Log — verify 4 entries
        15. Go to Dashboard — stats updated
        16. Export CSV — file downloads
        17. Go to Admin Panel — health shows 4 green dots
        18. Logout — goes to login page

  [ ] Fix every bug found during this walkthrough
  [ ] Test on mobile screen using Chrome DevTools
  [ ] Fix any mobile issues

  [*] COMMIT: "Day 25: Full integration test complete, all bugs fixed"

  PHASE 2 COMPLETE. Full stack app working end to end.


====================================================
PHASE 3 — AWS DEPLOYMENT + FINISH (Days 26-35)
====================================================


----------------------------------------------------
DAY 26 — AWS ECR: PUSH ALL 4 SERVICE IMAGES
Time: 3 hours
Goal: Docker images live on AWS ECR
----------------------------------------------------

  [ ] AWS Console → ECR → Create 4 repositories:
        fraudguard/auth-service
        fraudguard/transaction-service
        fraudguard/ai-scoring-service
        fraudguard/notification-service

  [ ] Login to ECR:
        aws ecr get-login-password --region ap-south-1 | \
          docker login --username AWS --password-stdin \
          YOUR_ACCOUNT_ID.dkr.ecr.ap-south-1.amazonaws.com

  [ ] For each service — build, tag, push:
        docker build -t fraudguard/auth-service ./services/auth-service
        docker tag fraudguard/auth-service:latest \
          YOUR_ACCOUNT_ID.dkr.ecr.ap-south-1.amazonaws.com/fraudguard/auth-service:latest
        docker push \
          YOUR_ACCOUNT_ID.dkr.ecr.ap-south-1.amazonaws.com/fraudguard/auth-service:latest

        Repeat for transaction, ai-scoring, notification services

  [ ] Verify all 4 images appear in ECR console

  [*] COMMIT: "Day 26: All 4 Docker images pushed to AWS ECR"


----------------------------------------------------
DAY 27 — AWS ECS: TASK DEFINITIONS
Time: 3 hours
Goal: ECS task definitions created for all 4 services
----------------------------------------------------

  [ ] AWS Console → ECS → Create Cluster
        Name: fraudguard-cluster
        Infrastructure: AWS Fargate
        Click Create

  [ ] Create Task Definition for auth-service:
        Family: fraudguard-auth-task
        Launch type: Fargate
        CPU: 0.25 vCPU
        Memory: 0.5 GB
        Container:
          Name: auth-service
          Image: your ECR image URL for auth-service
          Port: 3001
          Environment variables: add all .env key=value pairs
        Create task definition

  [ ] Repeat for all 3 remaining services:
        fraudguard-transaction-task (port 3002)
        fraudguard-scoring-task (port 3003)
        fraudguard-notification-task (port 3004)

  [!] For environment variables on ECS — do NOT put real secrets in
      the console in plain text. Use AWS Secrets Manager:
        AWS Secrets Manager → Store new secret
        Store MONGO_URI, JWT_SECRET, GEMINI_API_KEY as secrets
        Reference them in ECS task definition as secret references

  [*] COMMIT: "Day 27: ECS task definitions created for all services"


----------------------------------------------------
DAY 28 — AWS ECS: DEPLOY SERVICES + LOAD BALANCER
Time: 4 hours
Goal: All 4 services running live on AWS
----------------------------------------------------

MORNING — Create ECS Services
  For each task definition, create an ECS Service:
  [ ] ECS → Clusters → fraudguard-cluster → Create Service
        Launch type: Fargate
        Task definition: fraudguard-auth-task
        Service name: auth-service
        Desired tasks: 1
        Networking: select default VPC and subnets
        Security group: allow inbound on port 3001
        Click Create

  Repeat for all 4 services.

  [ ] Wait for all 4 services to show RUNNING status (5-10 min each)
  [ ] Get the public IP of each running task
  [ ] Test /health on each service IP

AFTERNOON — Application Load Balancer
  [ ] EC2 → Load Balancers → Create Application Load Balancer
        Name: fraudguard-alb
        Scheme: Internet-facing
        Create target groups for each service
        Path rules:
          /auth/* → auth target group
          /transactions/* → transaction target group
          /audit/* → notification target group
          /scoring/* → scoring target group

  [ ] Get ALB DNS name

  [*] COMMIT: "Day 28: All services deployed on ECS with ALB routing"


----------------------------------------------------
DAY 29 — FRONTEND DEPLOY: S3 + CLOUDFRONT
Time: 3 hours
Goal: React app live on AWS with CloudFront CDN
----------------------------------------------------

  [ ] Update client/.env for production:
        VITE_AUTH_URL=http://your-alb-dns.ap-south-1.amazonaws.com
        VITE_TRANSACTION_URL=http://your-alb-dns.ap-south-1.amazonaws.com
        VITE_AUDIT_URL=http://your-alb-dns.ap-south-1.amazonaws.com

  [ ] Build: cd client && npm run build

  [ ] AWS Console → S3 → Create Bucket
        Name: fraudguard-frontend-yourname (must be globally unique)
        Region: ap-south-1
        Uncheck "Block all public access"
        Enable static website hosting
        Index document: index.html
        Error document: index.html (for React Router to work)

  [ ] Upload dist/ folder contents to S3 bucket

  [ ] AWS Console → CloudFront → Create Distribution
        Origin domain: select your S3 bucket
        Default root object: index.html
        Create distribution (takes 10-15 minutes to deploy)

  [ ] Get your CloudFront URL: https://xxxxxx.cloudfront.net
  [ ] Open URL in browser — full app should load and work
  [ ] Test login, create transaction, full flow on live URL

  [ ] Update CORS in all backend services to allow CloudFront URL:
        cors({ origin: ['https://xxxxxx.cloudfront.net'] })
        Rebuild and redeploy affected services

  [*] COMMIT: "Day 29: Frontend live on S3 + CloudFront"


----------------------------------------------------
DAY 30 — GITHUB ACTIONS CI/CD PIPELINE
Time: 3 hours
Goal: Auto-deploy on every GitHub push
----------------------------------------------------

  [ ] Add secrets to GitHub repo:
        Settings → Secrets and variables → Actions
        Add: AWS_ACCESS_KEY_ID
        Add: AWS_SECRET_ACCESS_KEY
        Add: AWS_ACCOUNT_ID

  [ ] Create .github/workflows/deploy-auth.yml
        Copy template from Part 2.10 of this PRD
        Trigger: push to main + path: services/auth-service/**

  [ ] Create 3 more workflow files for other services

  [ ] Create .github/workflows/deploy-frontend.yml:
        Trigger: push to main + path: client/**
        Steps:
          1. npm run build
          2. aws s3 sync dist/ s3://fraudguard-frontend --delete
          3. aws cloudfront create-invalidation --paths "/*"

  [ ] Push to main → go to GitHub Actions tab
  [ ] Watch pipeline run automatically
  [ ] Green checkmark = working CI/CD

  [ ] Make a small change (update a comment), push, watch auto-deploy

  [*] COMMIT: "Day 30: GitHub Actions CI/CD pipeline complete"


----------------------------------------------------
DAY 31 — README + GITHUB CLEANUP
Time: 3 hours
Goal: GitHub repo looks like professional open source project
----------------------------------------------------

  [ ] Write complete README.md in repo root:
        # FraudGuard — AI Fraud Detection (Microservices on AWS)
        One-line description
        Live demo link (CloudFront URL)
        Architecture diagram image
        3 screenshots of the UI
        Services table (name, tech, port, purpose)
        AWS architecture bullet list
        Tech stack table
        Local setup steps (docker-compose up --build)
        Environment variables needed
        CI/CD explanation
        Future improvements
        Your name + LinkedIn

  [ ] Add /screenshots folder with 3 good screenshots
  [ ] Clean up code: remove all console.logs, test files, unused code
  [ ] Verify .env files are NOT in any commit: git log --all -- .env
  [ ] Count commits: should be 30+ meaningful commits
  [ ] Each commit message should be readable and specific

  [*] COMMIT: "Day 31: README and GitHub repo cleanup"


----------------------------------------------------
DAY 32 — DEMO VIDEO + LINKEDIN POST
Time: 2-3 hours
Goal: Project publicly visible and shareable
----------------------------------------------------

  [ ] Record 4-5 min demo on Loom (loom.com — free):
        Show in this exact order:
        1. Login screen (10 seconds)
        2. Dashboard with charts and stats (30 seconds)
        3. Transaction Feed — show filters working (30 seconds)
        4. Click "Generate Mock Transaction" — watch it load (15 sec)
        5. Wait for Gemini scoring — show score appearing (20 seconds)
        6. Click the scored transaction — show gauge chart (30 seconds)
        7. Read AI reasons out loud — explain them
        8. Add a note and click Reject
        9. Go to Review Queue — approve one more
        10. Go to Audit Log — show both entries logged (20 seconds)
        11. Go to Admin Panel — show 4 green service health dots
        12. Briefly show CI/CD on GitHub Actions (10 seconds)

  [ ] Upload to YouTube (Unlisted)
  [ ] Add YouTube link to README

  [ ] Update LinkedIn:
        Add FraudGuard to Featured Projects
        Write a post (copy this):

        "Just finished building FraudGuard — an AI-powered fraud
        detection system on AWS using microservices architecture.

        Tech: React.js + Node.js + MongoDB + Docker + AWS ECS,
        SQS, S3, CloudFront + Gemini AI + GitHub Actions CI/CD

        4 independent microservices communicating via AWS SQS.
        Gemini AI scores every transaction for fraud risk in
        real-time. Deployed on AWS with auto-deploy on every push.

        Live: [cloudfront-url]
        GitHub: [github-url]

        #AWS #Microservices #MERN #Docker #Fintech #React #Gemini"

  [ ] Add project to resume (copy bullet points from Part 3.10)

  [*] COMMIT: "Day 32: Demo video and LinkedIn post done"


----------------------------------------------------
DAY 33 — LEETCODE CATCHUP + TECHNICAL INTERVIEW PREP
Time: 3 hours
Goal: Ready for JP Morgan coding round
----------------------------------------------------

  [ ] Do 5 LeetCode problems today (Medium level):
        Two Sum, Best Time to Buy Stock, Maximum Subarray
        Valid Parentheses, Merge Two Sorted Lists

  [ ] Review and be able to explain these concepts cold:
        What is JWT — how does it work step by step
        What is bcrypt — why not SHA256
        What is REST API — status codes 200/201/400/401/403/404/500
        What is CORS — how do you fix it
        What is async/await vs callbacks vs promises
        What is MongoDB index — why did you add it
        What is Docker — what is a container vs a VM
        What is ECS Fargate vs EC2 — why did you choose Fargate
        What is SQS — difference between Standard and FIFO queue
        What is CloudFront — what is a CDN

  [*] Write answers in a notes file so you can review them

  [ ] Do NOT touch any code today — rest your brain from building


----------------------------------------------------
DAY 34 — PROJECT INTERVIEW PREP
Time: 3 hours
Goal: Can explain every part of project confidently
----------------------------------------------------

  [ ] Practice these answers OUT LOUD. Time yourself.
      Record yourself on phone. Listen back.

        "Tell me about your project" → MAX 2 minutes
          Start with the problem, then solution, then tech, then results
          Example start: "FraudGuard is a real-time fraud detection
          system I built to simulate how banks like JP Morgan detect
          fraudulent transactions. The system uses..."

        "Why microservices architecture?" → 45 seconds

        "How does the AI scoring work?" → 60 seconds

        "Why SQS for service communication?" → 45 seconds

        "Walk me through a transaction from creation to scoring" → 90 seconds
          User creates transaction →
          Transaction service saves it to MongoDB →
          Sends message to SQS scoring queue →
          AI Scoring worker picks it up →
          Calls Gemini API with transaction data →
          Gets back risk score + reasons →
          Updates transaction in MongoDB →
          Sends message to notification queue →
          Notification service creates audit log

        "What was the hardest challenge?" → 45 seconds

        "What would you improve with more time?" → 30 seconds

  [ ] Know these files line by line — be ready to share screen:
        services/ai-scoring-service/src/services/aiScoringService.js
        services/ai-scoring-service/src/workers/scoringWorker.js
        services/auth-service/src/middleware/authMiddleware.js
        services/transaction-service/src/services/queueService.js

  [ ] Open the live app on your phone
        If an interviewer asks "can you show me the app" → show it instantly


----------------------------------------------------
DAY 35 — APPLY + FINAL CHECKS
Time: 2 hours
Goal: Resume updated and applications submitted
----------------------------------------------------

  [ ] Final resume check:
        SkySecure Technologies at top of experience
        FraudGuard at top of projects
        All bullet points have metrics and action verbs
        GitHub + LinkedIn + Portfolio links work
        Zero spelling mistakes (run through Grammarly)
        One page PDF

  [ ] Apply to JP Morgan:
        https://careers.jpmorgan.com
        Search: Software Engineer Bengaluru
        Search: UI Developer Bengaluru
        Search: Full Stack Developer India

  [ ] Also apply to these today (cast wide net):
        Deutsche Bank India Technology — careers.db.com
        Goldman Sachs Bengaluru — goldmansachs.com/careers
        HSBC Technology India — hsbc.com/careers
        Barclays Technology Pune — search.jobs.barclays
        Morgan Stanley India — morganstanley.com/people

  [ ] Apply on LinkedIn too:
        Search "Software Engineer fintech Bengaluru"
        Filter: Date posted — last week
        Apply to top 10 results

  [*] FINAL COMMIT: "Day 35: Project complete — deployed and interview ready"


====================================================
DAILY HABITS — ALL 35 DAYS
====================================================

Every single morning (non-negotiable):
  [ ] 30 mins LeetCode before anything else
  [ ] Open this file, read today's tasks only
  [ ] Open VS Code + 4 terminal tabs

Every single evening:
  [ ] Git commit and push — EVERY day without exception
  [ ] Write 2 sentences: what you built, what was hard
  [ ] Read tomorrow's tasks so you wake up knowing what to do

Every Sunday:
  [ ] Test the full app end to end as a new user
  [ ] Fix any broken things before continuing
  [ ] Do 5 LeetCode instead of 2 (catch-up day)


====================================================
35-DAY PROGRESS TRACKER
====================================================

PHASE 1 — BACKEND (Days 1-14)
  Day 1  [ ] Project setup + MongoDB Atlas
  Day 2  [ ] Auth service — User model
  Day 3  [ ] Auth service — Register + Login
  Day 4  [ ] Auth service — JWT middleware + complete
  Day 5  [ ] Transaction service — setup + model
  Day 6  [ ] Transaction service — SQS + Stripe setup
  Day 7  [ ] Transaction service — Stripe Webhook handler
  Day 8  [ ] Transaction service — CRUD + Mock fallback
  Day 9  [ ] Gemini API setup + test
  Day 10 [ ] AI Scoring service — SQS worker
  Day 11 [ ] Notification service — audit log
  Day 12 [ ] Docker + docker-compose
  Day 13 [ ] Backend end-to-end test
  Day 14 [ ] AWS account + real SQS queues

PHASE 2 — FRONTEND (Days 15-25)
  Day 15 [ ] React setup + Redux + Login page
  Day 16 [ ] Sidebar + Navbar + Layout
  Day 17 [ ] Table + Badge components
  Day 18 [ ] Transaction Feed page
  Day 19 [ ] Transaction Detail page
  Day 20 [ ] Review Queue page
  Day 21 [ ] Audit Log page
  Day 22 [ ] Admin Dashboard + Charts
  Day 23 [ ] Polish + Loading + Mobile
  Day 24 [ ] System Health page
  Day 25 [ ] Full integration test

PHASE 3 — AWS DEPLOY (Days 26-35)
  Day 26 [ ] Docker images → AWS ECR
  Day 27 [ ] ECS Task Definitions
  Day 28 [ ] ECS Services + Load Balancer
  Day 29 [ ] Frontend on S3 + CloudFront
  Day 30 [ ] GitHub Actions CI/CD
  Day 31 [ ] README + GitHub cleanup
  Day 32 [ ] Demo video + LinkedIn post
  Day 33 [ ] LeetCode + Technical prep
  Day 34 [ ] Project interview prep
  Day 35 [ ] Apply to JP Morgan + others


====================================================
RESOURCES
====================================================

Gemini API key (free, no card needed)
  https://aistudio.google.com/app/apikey

MongoDB Atlas free cluster
  https://cloud.mongodb.com

AWS Free Tier
  https://aws.amazon.com/free

Docker Desktop
  https://www.docker.com/products/docker-desktop

Postman API testing
  https://postman.com

LeetCode daily practice
  https://leetcode.com

Recharts documentation
  https://recharts.org/en-US/api

Tailwind CSS documentation
  https://tailwindcss.com/docs

Loom free screen recording
  https://loom.com

Google Generative AI Node SDK
  https://ai.google.dev/api/node

JP Morgan careers
  https://careers.jpmorgan.com


====================================================
END OF FRAUDGUARD MICROSERVICES PRD
Stack: MERN | AI: Gemini Free | DB: 1 MongoDB | Cloud: AWS
Timeline: 35 days | 2-3 hours/day after work
Build it. Deploy it. Own it.
====================================================
